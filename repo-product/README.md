# specquill (monorepo example)

This fixture repo mimics a product monorepo: source code lives in `src/`,
and the SpecQuill workspace lives in the `docs/specs/` subfolder — opened as
a project with `content_root: docs/specs`.
